server:
  port: 18888

spring:
  application:
    name: customer-stock-report-backend
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${POSTGRES_JDBC_URL:jdbc:postgresql://localhost:5432/saylove02}
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:}
